class GICP:
    def __init__(self, iters):
        pass

    def estimate(self, kcam, source_points, target_matcher):
        def _closure():
            cov = compute_covariance(source_points)


            target_points, source_points

            torch.mean(torch.log(pdf()))
